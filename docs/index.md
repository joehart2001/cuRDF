# cuRDF

GPU-accelerated radial distribution functions using Toolkit-Ops + PyTorch, with adapters for MDAnalysis and ASE.

- Quickstart: basic install and first RDF
- API: core functions and CLI flags

Install:
```
pip install -e .[analysis]
```
